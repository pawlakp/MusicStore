@using MusicStore.Domain.Entities
@model IEnumerable<Album>

@{
    ViewBag.Title = "Albumy";
}

@foreach (var p in Model)
            {
                <div>
                    <h3> @p.album_name</h3>
                    <h4> @p.price </h4>
                </div>
            }

